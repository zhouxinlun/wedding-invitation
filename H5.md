# 小程序与 H5 共用一份请柬

小程序在 `miniprogram/`，网页在 `web/`，共用婚礼内容、祝福云函数、数据库、照片和动图。继续在这个仓库协作，无需重新做一份项目。保留小程序支持不代表可以通过个人主体的 UGC 类目审核。

## 当前验收

2026-09-09：H5 测试版已部署到现有 CloudBase，应用 `wedding-invitation-h5`，版本 `wedding-invitation-h5-001`，47 个静态文件上传成功。73 组本地检查通过。真实匿名登录、H5 投稿/表情/两张照片/回复、云相册和合影动图已验证；小程序真实读取到 H5 投稿，作者权限独立。横竖照片分别为 1280×640 和 640×1280。测试内容已清理，原有 5 条祝福保留。网页系统删除确认框曾卡住浏览器自动化，用户手动确认后照片被移除；整帖清理使用现有维护接口。回复和整帖删除交互另有 DOM 自动检查。

390px 视口无横向溢出，祝福列表固定 320px 并内部滚动。两部真实手机的操作仍需验收；正式域名、备案、HTTPS 自定义域名绑定待完成。

## 本地运行与发布包

2026-09-13：H5 首页改为「我们」，打开后先展示现有的静音循环合影短片，菜单为「我们 / 相册 / 祝福 / 流程 / 赴宴」。首屏保留新人姓名、婚礼日期、典礼时间和赴宴入口。原 `#invitation` 链接兼容跳转到 `#us`，相册等章节深链接继续有效。小程序保留原六个章节。

分享封面使用已确认的 `couple-red-natural-v2.jpg`。网页内页和构建后的根地址均包含静态 Open Graph 标题、简介和封面地址；分享优先使用系统文件分享面板（图片＋邀请文字＋链接）；不支持时显示同一封面供长按保存，并复制根链接，复制时不携带当前章节或访客 URL 参数。分享元信息的正式地址为 由 `wedding.shareUrl` 设置，目前为 `https://xinni1006.mengmeng.site/`，封面随静态网站部署，无临时签名依赖。本次静态包尚未部署；ICP备案完成并切换域名时，需同步修改分享配置和 HTML 元信息。

当前没有公众号，也未接入微信 JS-SDK 签名服务。上述元信息供支持网页预览的平台读取，**不保证微信直接粘贴链接会显示指定缩略图**；现阶段可用「封面图片 + 请柬链接」发送。浏览器允许时短片静音循环播放，限制自动播放时保留原合影，并在下一次真实触屏或键盘操作时恢复播放。

2026-09-13 界面优化：移除 H5 合影上的“再看一次”按钮，短暂媒体/网络失败在可见状态下最多自动重试三次，联网与返回页面可恢复；离屏、后台或打开相册时暂停。四周增加固定在内容背后的玫瑰花藤及小喜签，手机端收窄到边缘，不拦截点击。相册使用普通“关闭 ×”文字按钮和圆形翻页键，手机翻页键放到底部，保留照片自然比例、滑动翻页与键盘关闭。

本次验证：完整 `npm test` 通过；新增播放器运行检查覆盖浏览器拒绝自动播放、网络重试上限、联网恢复、相册/后台暂停及迟到请求。真实浏览器中 81.5 秒短片从 51.04 秒连续播放 42.74 秒后回到 12.31 秒，确认自然循环且无重播按钮。375×667、390×844 和 1280×720 布局已检查，无横向溢出；关闭按钮触区为 113×46px，云照片保持比例，翻页及关闭有效。更新已构建到本地 `dist/h5`，尚未同步至公网服务器。

本次本地验证：390×844、375×667 手机视口与 1280×900 桌面布局；根地址直接进入我们，云端 81.5 秒短片已静音循环播放，切换到相册后合影播放器正常暂停。五个菜单、旧锚点、分享封面原比例展示及链接复制已在浏览器验证，祝福列表仍为 320px 内部滚动。完整检查通过；后续锚点兼容与小屏浮钮调整的 H5 定向检查通过。本次未部署静态托管，未验证微信手机中的原生链接卡片。

使用 Node.js 22 或更高版本：

```sh
npm run setup
npm test
npm run dev:web
```

打开 `http://127.0.0.1:8765/`。服务器仅提供构建好的 `dist/h5/`，不会暴露云函数源码、凭据或本地原片。`npm run build:web` 会重新生成发布包，修改后重新构建并刷新浏览器。

在 CloudBase 的「静态网站托管 → 本地项目上传」上传 `dist/h5/` 的内容，或将其中内容压缩成 ZIP（ZIP 根目录直接包含 `index.html`）。选择其他框架，目标目录和构建产物目录填 `./`，安装命令、构建命令留空，部署路径为 `/`。这是已经构建好的静态网站，无需购买另一台服务器。

## 当前云环境

- 环境：`wanshifuyue-d4ghbtrbi0e9bd01b`，上海。
- 小程序 AppID：`wx0d45570c72a490e7`。
- 云函数：`weddingBlessings`，两端使用相同动作和数据集合。
- H5 使用官方 CloudBase JS SDK 的匿名登录。称呼由宾客填写，不静默读取微信昵称、头像或手机号。
- 同一浏览器保留访客身份；更换设备、浏览器或清除数据后，可能无法删除旧身份留下的内容。H5 身份与微信 OpenID 不自动合并，不能按相同昵称认领历史内容。
- 数据库保持客户端禁止直接读写；云存储保持仅创建者可读写。云函数只签名部署清单中的相册、动图，以及已公开祝福的照片。
- 不把私钥、API Key、管理员 OpenID 或临时图片签名放入网页和仓库。前端环境 ID 是公开配置。

网页第一次访问需要「身份认证 → 登录方式 → 允许匿名登入」。在「HTTP 网关 → 跨域设置」加入网页的准确域名；本机开发使用 `127.0.0.1:8765`。正式域名确定后补充正式来源。

### 2026-09-13：外部服务器域名接入

当前公开站点为 `https://xinni1006.mengmeng.site/`，静态目录 `/opt/invitation-app`，Nginx 配置 `/etc/nginx/nginx.conf`。网站通过浏览器直接访问现有 CloudBase，不需要把这个前端域名绑定到 CloudBase 网关或修改其 DNS 指向。

已在本环境「HTTP 网关 → 跨域设置」添加 `xinni1006.mengmeng.site`，保留其他安全域名及现有身份、存储权限。此前新域名的匿名登录预检返回 403，而本地预览来源返回 204；添加后公网预检返回 204，登录和云函数请求返回 200。

实际公网浏览器已验证：81.5 秒合影短片播放（720×1080）、云相册照片显示（853×1280）、已有亲友祝福和祝福照片读取成功。本次未提交测试祝福，也未重新部署本地前端代码；服务器静态包仍是此前的六菜单版本。

已补充该域名的 HTTP → HTTPS 跳转，保留路径和查询参数。最新配置备份位于服务器 `/opt/invitation-backups/20260913-172558-http-redirect/nginx.conf`，Nginx 配置检查与重载通过。后续修改 Nginx 时需保留此跳转。新增或更换前端域名时，须同步更新 CloudBase 的安全域名列表，并实际验证登录与媒体加载。

云函数权限保留其他函数原规则，仅为请柬函数允许已建立身份的访客调用：

```json
{
  "*": { "invoke": "auth != null && auth.loginType != 'ANONYMOUS'" },
  "weddingBlessings": { "invoke": "auth != null" }
}
```

H5 的发送、回复和删除复用云端作者校验、事务、限流与重试去重。前台每 20 秒更新祝福；后台暂停轮询。照片按自然比例缩放至最长边 1280px，服务端重新编码。只对一级祝福展示飘雪。

## 你现在办理域名的步骤

1. 登录腾讯云，用你准备持有域名的账号完成实名认证。
2. 进入「域名注册 → 信息模板」，新建个人模板，按你的真实姓名、证件和联系方式提交实名审核。腾讯云目前要求模板通过审核后才能注册域名。
3. 在「域名注册」搜索喜欢的名字，优先选择容易读写的 `.com`，看清首年及续费价格，再购买。把最终域名发给我即可，暂时不用填写 DNS 记录。
4. 在 CloudBase「环境管理 → 备案管理」检查当前套餐能否作为备案资源。官方当前条件包括个人版或以上、剩余有效期超过六个月，并开启云托管固定 IP；是否已满足、是否需续费，以账号页面为准，不先额外购买服务器。
5. 按腾讯云 ICP 备案流程，如实填写个人婚礼请柬网站的用途和互动功能，完成资料与身份核验。备案需平台及管局审核，应尽早提交。
6. 域名及备案准备好后，在「静态网站托管 → 基础配置」绑定自定义域名，按页面给出的目标添加 CNAME、配置 HTTPS，并把域名加入 Web 安全来源。DNS 目标以绑定时控制台实际显示为准。

默认测试域名是 `wanshifuyue-d4ghbtrbi0e9bd01b-1483826843.tcloudbaseapp.com`。它有访问限制和提示页，不能当作长期发给亲友的正式地址；最终请柬使用你的 HTTPS 域名。

普通 H5 可以在微信内打开、转发链接，也可分享请柬长图。自定义微信分享标题、缩略图等 JSSDK 能力另需符合条件的公众号配置，当前没有承诺接通；不影响基本链接访问。

官方步骤：[域名注册](https://cloud.tencent.com/document/product/242/39039)、[CloudBase 备案条件](https://cloud.tencent.com/document/product/876/128405)、[默认域名限制](https://docs.cloudbase.net/service/introduce)、[匿名登录](https://docs.cloudbase.net/authentication-v2/method/anonymous)、[函数安全规则](https://docs.cloudbase.net/cloud-function/security-rules)。

2026-09-13 本轮：分享入口只保留“分享这份喜悦”。浏览器原生分享需要文件支持及真实点击；用户取消不会再次弹窗，文件不支持或分享失败时回到新图＋复制链接，不保证微信保留全部分享字段、自动发送或生成固定卡片。旧长图 PNG、旧封面和旧小程序分享卡已移除；相册新封面直接读取主包文件，无云签名依赖。心动如初与囍色倾心对调。相册四边复用十款藤蔓，逐次展开，保留喜字和一级祝福飘雪。五个模块使用同一双名花藤徽记，并提供多尺寸 favicon.ico。

昵称：当前为 CloudBase 访客身份，并非微信昵称头像授权。来宾称呼首次填写后在当前浏览器记忆，祝福和回复共用可修改的小落款；不按称呼认领身份或历史内容。无公众号网页授权时不伪造微信用户资料。

## 背景音乐与越框动作

2026-09-13 已接入用户提供的《Married Life》钢琴演奏版。由本地 MP4 `085.钢琴演奏：《Married Life》飞屋环游记 O_爱给网_aigei_com.mp4` 提取 AAC 音轨，保存为 `web/media/married-life-piano.m4a`，约 3 分钟，保留原音轨、不重新压缩。`miniprogram/wedding.js` 的 `music.webFile` 已指向该文件。Michael Giacchino 标为作曲者，不把这份钢琴演奏版标为电影原声录音。

运行 `npm run build:web` 后，构建仅包含明确配置的音频，不会把媒体目录所有原片打包。当前音乐入口统一在底部菜单右侧，以小唱片显示播放状态；手机与桌面保持同一位置。首次点击页面、触摸结束或按 Enter/空格时，在同一用户手势中调用播放，默认音量 28%，循环播放；不承诺在微信完全没有页面交互时有声自动播放。手动暂停后，其他点击不会重新开启。切换章节不重头播放，离开页面暂停，返回后恢复此前开启的音乐。浏览器拒绝播放或音频加载失败时，按钮提供重试。红色囍字灯笼改为悬挂在合影左侧，不再随窄屏花园装饰收窄、淡化。

公主抱越框场景已作为独立 8 秒 H5 资源接入，详见 `COUPLE-MOTION.md`。它与原云端合集连续轮换。9 月 14 日当前资源为约 4.03 MB 的 `couple-princess-popout-hd.mp4`，有效画面提升到 720×1080，并清理半透明头纱、裙边混入的灰底颜色；原片快速动作的运动模糊仍有保留。通过新文件名读取以免缓存旧片。构建后通过站点自身域名读取，部署时须完整同步 `dist/h5`。此前 1.87 MB 的 stable 版本随 afdee43 部署记录见下方。

相册详情的四条花藤每条展开 2.4 秒，依次延迟 0、0.2、0.4、0.6 秒，总共 3 秒完成，继续复用十款素材。构建后的 HTML 使用文件内容哈希标记本地 JS/CSS 版本，避免服务器七天缓存使来宾读取旧脚本；源 HTML 不写死发布日期。

## 2026-09-14 双名烫金封蜡

五个模块顶部使用新的暗红封蜡，保留双名花藤，增加不规则蜡边、立体阴影和金色压印。素材 `miniprogram/assets/couple-wax-seal.jpg` 为 320×320、66,533 字节；CSS 沿蜡边裁切并添加阴影。源图带棋盘格背景，并非真实透明 PNG，因此外轮廓使用 CSS clip-path，避免把背景作为透明像素展示。音乐徽章和 favicon 继续复用原头像。

素材由内置图像生成工具制作，提示词要点：沿用双名花藤徽记，制作逼真的深酒红色手工火漆封蜡，不规则熔蜡边缘、压印金色书法“zhouxinlun / lixiaoni”、细金花藤和暗红玫瑰，正面视角，适合请柬顶部小尺寸展示。原始生成图、轮廓及制作记录保存在本机 `../work/wax-seal-20260914/`。实际检查五个模块加载同一素材，以及 390×844 和 666×916 的首屏居中与边缘显示。


## 2026-09-13 H5 发布记录

- 代码版本：`afdee43b20e259b3ee8c97966cc8c86ca68002ba`，已推送 `feature/h5-support`。
- 公网：[打开请柬](https://xinni1006.mengmeng.site/)。发布 `dist/h5` 的 51 个文件到 `/opt/invitation-app`，逐个通过 HTTPS 下载并核对 SHA256 一致；未修改共享服务器其他站点。
- 发布前的完整静态站备份：`/opt/invitation-backups/20260913-afdee43/site`。同目录保存旧 Nginx 配置、发布归档、文件哈希清单及部署记录，均在公开目录之外。
- 保留 HTTP → HTTPS 跳转和既有 Nginx 配置。HTML 返回 `Cache-Control: no-cache`，页面 JS/CSS 使用内容哈希版本；媒体 Range 请求返回 206。
- `npm test`、`npm run check:popout-matte` 与提交前凭据扫描通过。公网实际浏览器验证：新 8 秒短片与 81.5 秒云视频解码，云相册 853×1280 原图加载，四周花藤 3 秒内展开，既有亲友祝福读取，180.58 秒音乐正常播放并在切换相册后继续。
- 本次为静态 H5 发布，不涉及小程序审核，也未重新部署云函数或修改祝福数据。新红色封面由 H5 本地资源直接展示，云函数中的旧照片对象清单尚未同步删除。

回退时先把当前 `/opt/invitation-app` 移到独立目录保留，再将上述 `site` 备份恢复到 `/opt/invitation-app`，核对首页和云媒体后再处理后续版本。本轮无需修改 Nginx 配置即可回退。

## 2026-09-14 H5 发布记录

- 代码版本 `171d895f12b2f400e968825567c2eb085627430d`，已推送 `feature/h5-support`；包括高清越框短片和五个模块的双名烫金封蜡。
- 发布 52 个静态文件，共 9,255,977 字节。全部从公网 HTTPS 下载，SHA256 与本地发布清单一致。
- 发布前完整站点备份：`/opt/invitation-backups/20260914-171d895/site`。同目录保存发布归档、清单、部署记录和原 Nginx 配置。本次未修改 Nginx、云函数或亲友数据。
- 实际公网浏览器解码 1440×1080 打包短片，合成为 720×1080 透明画布；短片结束后恢复 81.5 秒云端互动合集，无播放器错误。五枚 320×320 封蜡素材均加载成功。
- 首页保持 `Cache-Control: no-cache`，新视频和音乐 Range 请求均返回 206。高清与边缘改善不等于消除原片自带的运动模糊，剩余限制详见 `COUPLE-MOTION.md`。
- 本机发布记录在 `../work/20260914-171d895/`，包括部署脚本、归档和逐文件 HTTPS 校验结果。回退方式沿用上一节，使用本次的 `site` 备份恢复前一线上版本。


## 2026-09-14 手机音乐与赴宴地图

复核无查询参数的公开 HTML，与前次发布包逐字节一致，并带内容哈希脚本版本。390px 下原花园音乐徽章被 CSS 隐藏，灯笼只有约 27px 宽且随边缘装饰淡化；这些可复现的布局差异是本次修复依据，未将未访问过的用户手机缓存认定为根因。

- 底部五个章节旁追加独立音乐按钮，替换原上方浮钮与花园内的音乐入口。首次页面手势启动一个播放器，手动暂停不被随后点击覆盖。参考 [浏览器有声播放限制](https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Autoplay)。
- 赴宴以地图卡片作为滚动目标，使用未受入场动画影响的布局位置居中，支持菜单、首页饭店链接、流程中的两家链接和直接 `#journey` 地址。空间不足的矮屏对齐卡片顶部并保留正常页面滚动。典礼 11:08、到场 10:40、日期及日历下载放到卡片下方。
- `web/journey-map.js` 用 [Leaflet 1.9.4](https://leafletjs.com/reference.html) 显示地图，库及 CSS 随静态包发布。使用 OpenStreetMap 标准底图，保留可见署名、浏览器缓存和来源信息；只有赴宴进入可见范围时请求所需图块，不预取其他地区、不下载离线地图。遵循 [图块使用规则](https://operations.osmfoundation.org/policies/tiles/)。底图网络失败有状态提示，外部导航仍可用。
- 三个目的地仍由 `miniprogram/wedding.js` 统一维护，保留 GCJ-02 坐标。H5 使用 `coordtransform 2.1.2` 将目的地转换到 OSM 所用 WGS84，并与浏览器 Geolocation 的当前位置计算直线距离；导航时将起点转换回 GCJ-02，避免位置偏移。
- 进入赴宴时按需请求一次手机定位，切换地点复用本次位置；支持拒绝、超时及手动重试，不虚构访客位置。坐标仅留在当前页面内存中，不上传祝福云函数或保存到本地存储。标准定位需要 HTTPS 与浏览器授权；拒绝时地图保留目的地，[高德导航](https://lbs.amap.com/api/uri-api/guide/travel/route)可在手机内再取得当前起点。
- 检查覆盖首次点击播放、手动暂停、返回续播、卡片居中不受 24px reveal 位移影响、地点按钮与首页饭店链接同步，以及实际 Leaflet 初始化、三个目的地、坐标转换和定位成功/拒绝/超时路径。模拟定位用例是运行校验，不替代真机定位授权验收。

### 本轮发布结果

- 版本 `494fc2aa16aec68fdab16590de26b8ff899be2e9` 已推送 `feature/h5-support` 并部署。62 个静态文件，共 9,441,280 字节，全部通过公网 HTTPS 哈希比对。
- 备份 `/opt/invitation-backups/20260914-494fc2a/site`；发布前已核验原站点所有文件仍与上一版本清单一致。未改 Nginx、云函数、祝福数据或小程序发布状态。
- 公网 390×844 视口：首次点击赴宴后，音乐实际播放；卡片顶部约 56px、高 644px，在底部菜单上方居中；地图图块加载正常，无横向溢出。375×667 矮屏使用 140px 地图，卡片顶部约 11px，保留页面正常滚动。首页和脚本沿用 HTML 重新验证、JS/CSS 内容哈希机制；音乐 Range 返回 206。
- 本地实际浏览器观察到饭店、新郎家地图和对应标记；自动检查验证三地点切换、导航起终点和定位成功/拒绝/超时。公网自动化在触发定位请求后部分点击未触发页面事件；原生应用弹窗无法由当前工具读取，因此未声称完成微信真机的定位授权与全部触摸验收。
- 本机完整发布清单、备份脚本与公网校验结果在 `../work/20260914-494fc2a/`。如需回退，沿用前述目录替换步骤，恢复本轮 `site` 备份。

## 2026-09-14 音乐预加载与底图挂起诊断

- `music.js` 原为 `preload=none`，改为 `preload=auto` 并在监听就绪后调用 `load()`。本地真实浏览器进入页面、尚未点击时已缓冲完整录音（180.58 秒），首次点击后开始播放。手机可根据网络和省流设置忽略预加载提示；有声播放继续依赖真实手势，手动暂停不会被其他点击覆盖。
- 国内服务器两次请求当前 `tile.openstreetmap.org` 可见图块均连接超时（一次显式 IPv4），本地浏览器同图块可正常显示。这证明当前底图链路有网络可达性差异，不是 CloudBase 相册资源问题；未直接读取用户手机网络日志，不能据此认定具体运营商或手机权限原因。
- 原 Leaflet 只处理成功/错误事件，连接一直挂起时没有加载期限。增加 10 秒等待期限、独立“重试地图”按钮，保留可用的导航链接；重试地图不重复索取 GPS。按当前图块追踪状态，排除切换地点后旧请求的干扰，成功或卸载后清理计时器。
- 回归检查使用实际 Leaflet，模拟图块永不返回，已先复现原版永远显示“正在展开地图”，再验证超时、部分失败、重试成功、旧请求隔离和地点切换。音乐检查覆盖预加载与原播放/暂停行为。
- **尚未解决的外部依赖**：本次仍保留原 OSM 底图，超时提示不代表底图网络已恢复。已向用户请求高德 Web 端（JS API）Key 及安全密钥，收到后再替换与验证国内地图。申请见[高德准备步骤](https://lbs.amap.com/api/javascript-api-v2/prerequisites)，生产环境按[官方安全密钥说明](https://lbs.amap.com/api/javascript-api-v2/guide/abc/jscode)通过服务端代理保护安全密钥。
- 已发布 `00364a41926d27014297ae3b82b5080358e4fc84`，62 个静态文件、9,442,125 字节全部通过公网 HTTPS 哈希比对。服务器备份 `/opt/invitation-backups/20260914-00364a4/site`，本地发布记录 `../work/20260914-00364a4/`。Nginx 与云端数据未变更。
- 线上真实浏览器在未点击时读取到 `preload=auto`、`paused=true`、`readyState=4`，缓冲 180.58 秒；音乐 Range 返回 206。全套 `npm test` 通过。底图挂起路径通过实际 Leaflet 的受控请求用例验证，尚未完成用户微信真机与国内替代底图验证。

## 2026-09-14 手机背景音乐压缩

- 由原始提取音轨生成 `web/media/married-life-piano-mobile-v1.m4a`：AAC-LC、48 kbps、44.1 kHz、单声道，保留完整三分钟。实际文件由 2,929,930 字节降至 1,124,084 字节（减少 61.63%）；编码补齐后时长 180.605 秒。
- 命令：`ffmpeg -i web/media/married-life-piano.m4a -map 0:a:0 -map_metadata -1 -vn -c:a aac -b:a 48k -ac 1 -ar 44100 -movflags +faststart web/media/married-life-piano-mobile-v1.m4a`。原音轨保留在仓库，发布包只包含配置指向的压缩版。
- `moov` 位于文件头、媒体数据之前，完整解码无错误；峰值 -0.3 dB，未出现数字削波。独立文件名配合已有配置内容哈希，避免手机继续读取旧音频缓存。
- 实际浏览器用真实 `music.js` 与压缩音频验证：本地服务器限速约 16 KB/s，首次点击后播放正常；当时只缓冲 28.3 秒，尚未下载完整 180.6 秒，证明支持边加载边播放。音乐与 H5 回归检查通过。该模拟不代替不同手机和运营商的加载时延实测。
