# 相册云存储

> 当前更新（2026-09-09）：已移除独立 AI 影像菜单、占位页及播放器；下文影像相关记录仅为历史。本次保留“我们”的双人招手动图。

2026-09-08：正式 AppID wx0d45570c72a490e7，实际连接环境 wanshifuyue-d4ghbtrbi0e9bd01b。

64 张展示照片和 7 张相册封面已上传至 wedding-media/photos/，按文件 SHA-256 命名；共 7,807,846 字节。下载后逐张校验与本地文件一致。原封面、首页缩略图继续保留在轻量主包，照片没有重新生成。原始文件也保留在本地源码和网页离线预览中。

小程序打开一本相册时，复用 weddingBlessings 云函数的 album 操作。服务端仅允许 albums.json 中部署确认的照片，忽略客户端传来的文件 ID。先检查微信云端身份和正式 AppID，再生成一小时临时链接。页面在链接过期后返回、放大或重试时刷新链接；图片按需加载，可点击放大。祝福照片在处理完成后直接公开，保留作者归属与服务端照片校验、五分钟签名。

存储保持“仅创建者可读写”，未改为全桶公开。云平台拒绝了两次按目录自定义规则，最终方案不依赖这些规则。未签名的相册图片 HTTP 请求实测返回 403。临时链接在有效期内可由持有人访问，不能把临时签名视为防截屏或防转发。

相册原片目录通过 project.config.json 的 packOptions.ignore 排除上传，五个原有相册页面保留。官方预览实际总包 1,491,303 字节，主包 1,489,709 字节；改造前总包 8,283,651 字节。

云相册历史预览码：exports/正式账号-云相册-预览码.jpg；最新含祝福飘雪的预览码见 PUBLISH.md。正式开发版本是否上传、体验版及提审状态仍需公众平台核实，本次只完成云函数部署和预览。

验证：71 个云对象下载字节一致；七本相册真实签名成功；未知相册拒绝；真实新人身份与空祝福列表仍正常；模拟器云图按需显示与原生全屏查看已验证。左右滑动、另一位亲友微信账号、手机网络仍需真机验收。云函数运行时升级与祝福墙其他发布事项见 BLESSINGS.md。

更新素材：上传新的内容哈希路径，再更新 cloudfunctions/weddingBlessings/albums.json 和 miniprogram/wedding.js，部署云函数并重新预览。不要在代码中保存临时签名链接，不要改成任意用户输入文件 ID 的签名接口。云端对象清单见 exports/云端相册素材清单.json。

新娘敦煌成片由用户稍后提供。“一眼千年”当前仍未接入视频；收到成片后核对时长、格式和封面，再接入当前云存储并单独验证手机播放。没有上传参考片或已被否定的旧版本。

## 合影动图

“我们”的白纱合照动图已上传同一环境，使用 `motion.json` 中的固定云文件与 `usMotion` 签名动作；同样检查正式 AppID 和微信云身份，不接受用户提供的文件 ID。文件为720×1080 H.264、10秒静音循环、651790字节；本地与真实云下载SHA256一致。存储权限、相册清单与祝福数据库不变，具体见 COUPLE-MOTION.md。
