# 新娘敦煌短片接入

用户将在之后提供最终成片，本次仅完成云相册，见 CLOUD-MEDIA.md。不要把临时签名链接写入 openingFilm.url；云视频接入时应沿用服务端部署清单和临时链接刷新方式，另验播放。

当前入口为“一眼千年 · 李小妮个人映像”，明确显示敬请期待。openingFilm.enabled=false、url为空。没有使用旧视频，也没有新增生成费用。

成片要求沿用用户选择：女主一人、敦煌服化道与飞天动作、极致炫彩，16:9横屏，小于1分钟；先无声，配乐稍后另行确认。拿到文件后先核对画面与时长，导出适合手机播放的H.264 MP4及真实视频帧海报。

发布时将MP4与海报放到账号可用的HTTPS媒体存储，核对微信实际媒体访问和域名配置，再填写miniprogram/wedding.js中的openingFilm.url、poster，最后把enabled改为true。1分钟视频不直接塞入2MB主包。Web与小程序共用这份内容配置。

接入后的行为：请柬封面首先展示，影片位于邀请正文之后的独立影像区域。点击底部“影像”只滚动到该区域；点击播放按钮才静音播放，横屏内容保持完整。播放器提供微信全屏控制，顶部提供“返回影像”按钮；结束或关闭回到该区域，可再次播放。加载20秒未开始或媒体出错时提供重试与继续浏览请柬；切到后台关闭影院。进度按媒体实际时长计算；播放受浏览器限制时保留系统控件。

当前验证范围：未配置、打开、开始播放、进度、关闭、错误、超时、重试与后台退出逻辑。最终成片的画面质量、真实播放速度、手机横屏、CDN访问及真机验证须拿到视频后完成。
