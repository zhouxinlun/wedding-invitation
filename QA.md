# 验证记录 · 祝福墙本地候选与历史版本

> 当前更新（2026-09-09）：已移除独立 AI 影像菜单、占位页及播放器；下文影像相关记录仅为历史。本次保留“我们”的双人招手动图。

> **当前账号与云连接（2026-09-08）**：正式 AppID `wx0d45570c72a490e7`；实际关联环境 `wanshifuyue-d4ghbtrbi0e9bd01b`。微信开发者工具与真实云函数调用均已验证，祝福墙已接通。此前 `wx187416f73e2e53c3` 是接口测试号，旧的 CLI 上传成功记录不能证明正式开发版本上传。正式账号尚未上传开发版本、提审或发布。

## 2026-09-08 回复反馈与本人删除（当前）

用户明确问题发生在手机小程序。开发者工具复现空字段校验写入 replyError 但提示在滚动容器底部、画面看不到；没有声称已复现所有手机输入故障。改为原生 form + named controls 读取当前值，提示移动到固定发送区；处理完服务端写入后，等待旧轮询完成再刷新，避免旧列表覆盖新回复。

67组检查通过，新增本人删除权限、他人越权/伪造身份拒绝、重复删除、回复标记及禁止重试复活、删除后目标不可回复、两张照片并发删除按稳定键保留第三张、最后照片移除空帖、存储故障下不恢复公开图片；原生表单当前值覆盖滞后缓存、确认取消/锁定/删除后飘雪清理及强制刷新顺序。

真实表单控件由测试代码填入临时值，实际点击“送出回复”经新 form 事件提交成功，窗口关闭，正确显示“验证访客 回复 功能验证（临时）”。错误反馈、本人删除入口及单图删除确认/取消经 native UI 验证；实际云动作验证2张到1张、1条回复到0、整条测试祝福移除，现有3条内容未改，测试不再出现在飘雪。仅测试内容在验证中被创建/删除。两部真实微信手机及其相互权限仍待新码验收。

云函数部署成功；官方预览总包1519115字节、主包1517521字节，码为 exports/正式账号-回复与删除-预览码.jpg。Web同步删除演示及明确的小程序回复提示，仅代码检查，未绕过浏览器策略。原封面/合影动图保留；未执行正式上传、审核或发布。证据在 work/blessing-delete/。

## 2026-09-08 我们 · 微笑招手合影（前版）

61组逻辑检查通过；新增仅部署文件签名、身份与AppID校验、资源缺失、入屏去重、首帧后显示、暂停/恢复/过期刷新、卸载及失败重试。原红封面与64张相册照片未改。可灵一次5秒生成消耗50积分；正放接反放的10秒静音循环，经取帧检查后压缩为720×1080、651790字节。

真实云函数已部署 `usMotion` 动作；微信云下载HTTP200，SHA256与本地一致。实际小程序看到微笑与挥手，离屏两次读取进度均为3.257625，返回后继续播放。人为失效媒体返回403并正确回到原照片；原图预览打开白纱第2/4张，为重建播放器增加 loadedmetadata 就绪后播放，修正无首帧超时，重试恢复真实云视频已验证。移除开发者工具不支持的本地video poster属性，独立原图底层保留。故障探针的403为预期测试错误，未修改生产URL。

官方预览总包1506778字节、主包1505184字节；新码 `exports/正式账号-合影动图-预览码.jpg`。Web本轮仅代码和语法检查，未绕过既有浏览器URL策略；手机实际解码、移动网络与分享仍待真机验收。本轮没有正式上传、提审或发布。证据：`work/us-wave/`，设计和素材说明：COUPLE-MOTION.md。

## 2026-09-08 照片比例、正文表情与回复（前版）

58组本地检查通过；新增横/竖/小图压缩参数、比例、正文表情、回复关系/伪造目标/并发/幂等/删除/限流/容量、加载锁定与不明结果恢复，明确验证只有一级正文进入飘雪。无新集合和索引，云函数已更新。

实际云验证：320×180与120×240合成图上传后保持原尺寸，一级正文包含玫瑰；两条回复正确指向原作者和上一条回复作者。实际点击留言打开正确的回复面板，图像自适应小图与回复区可见。加载状态经实际调用读取，持续加载布局另外用原生临时状态检查。唯一测试根祝福及两条回复已移除，已有亲友内容保留。

官方预览成功，总包1,502,307字节、主包1,500,713字节，新码 exports/正式账号-祝福回复-预览码.jpg。开发者工具基础库重载出现一条 __subPageFrameEndTime__ 内部错误，业务云调用均成功；组件禁用按钮属性选择器警告已修正。Web当前仅脚本/样式验证，未绕过先前URL策略。两部真机、正式上传与发布未由本轮完成。证据在 work/blessing-replies/。

## 2026-09-08 祝福直接展示与飘雪（历史）

本轮替代旧的人工确认/三轨弹幕交互：新投稿完成图片处理后直接公开，取消额外勾选；只保留固定高度、时间倒序的亲友列表。新增6种不捕获触控的轻透明连接字串，重做寄信表单与照片文案。

50组本地检查通过。真实原生组件调用云端完成带合成测试图片投稿 → approved → 公共列表与飘雪 → 图片下载200且可解码 → 清理后公共列表为空，无管理员确认步骤。原生信纸表单与样例飘雪已实看；列表332px高，内部滚动332px不推动父页面，列表外滑动正常。模拟器Mock已清空，正式云端无示例投稿。Web本轮视觉检查受浏览器URL策略阻止，未绕过；本地脚本/样式检查通过。

官方正式账号预览成功：总包1,491,752字节、主包1,490,158字节。新码为 exports/正式账号-祝福飘雪-预览码.jpg。两部真机、云端运行时升级及正式开发上传/审核/发布未由本轮完成。证据位于 work/blessing-snowfall/；以下确认流程为历史记录。

## 2026-09-08 正式 AppID 与真实云连接

AppID `wx0d45570c72a490e7`、环境 `wanshifuyue-d4ghbtrbi0e9bd01b` 均经微信官方 CLI 和模拟器实际调用确认。两个私有集合、两条组合索引、存储创建者权限与新人环境变量已配置。

同一登录微信在模拟器中完成真实云函数身份识别、管理员识别、照片上传、待确认隔离、新人确认、公共列表和原生三轨弹幕展示、撤回后公共列表清空。客户端直接读两集合均返回 -502003；覆盖发布副本返回 -503002 / STORAGE_EXCEED_AUTHORITY。临时照片 URL 撤回后 HEAD 仍返回200，源文件及缓存失效未确认；不同微信的读取隔离尚未验证。没有声称两部手机验收通过。

云函数实际运行时为微信工具默认 Nodejs16.13，已调整512MB/60秒；升级至项目要求的Node18以上版本仍待完成。主包1,485,910字节，总包8,283,651字节；官方正式账号预览成功，未进行正式开发包上传或提审。连接配置调整后的11组祝福检查通过，原34组逻辑检查结果见历史记录。

证据：项目外 `work/invitation-formal-account/` 的 formal-cloud-integration.json、photo-flow-verification.json、blessings-live.jpg、preview-result.json。唯一测试投稿已撤回，正式公共列表为空。新人身份码仅保留在项目外私有文件及云端环境变量中。

## 历史：2026-09-08 亲友祝福墙（连接云端之前）

以下历史上传/预览数据来自接口测试号，不证明正式账号上传完成。



45组检查通过：原有34组 + 祝福墙11组。新检查运行真实云函数入口及图片编解码，替换的是 SDK 的身份、数据库和存储边界；涵盖作者隔离、管理员权限、待确认到公开、并发重复请求、失败重试、照片校验、限流、撤回及逐文件删除失败。记录位于项目外 work/invitation-blessings/checks.txt。

官方 WXML 编译8个文件、WXSS编译10个文件成功。修复一次 checkbox-group 模板标签错误；原生页面已恢复正常。去除组件 WXSS 中的标签选择器警告，并覆盖原生按钮预设尺寸。

官方 CLI 新预览成功：总包8,283,622字节，主包1,485,881字节，所有单包低于2MB。预览码为 exports/亲友祝福墙-小程序预览码.jpg。没有上传新开发版本；正式线上后台版本尚未核实。

实际 Web 检查：320/390像素均无横向溢出，320像素完整请柬七项菜单各44像素宽；弹幕三轨 CSS 动画及 pointer-events:none 已观察。填写窗口暂停弹幕；独立演示页发送后进入“我的心意”的待确认状态，经演示管理确认后进入公共列表；照片放大与关闭正常。演示仅使用内存中的明确示例数据，未发生云端提交。

微信开发者工具实际检查：原始封面与七项菜单、祝福信纸卡、真实未接通提示、称呼输入（微信昵称入口）、照片选择入口及同意展示说明。最后的按钮尺寸修正后首页悬浮按钮显示为紧凑胶囊。微信手机上的昵称快捷填入、照片授权及传输、两部手机同步仍未验证。原生已批准内容弹幕依赖真实云数据，尚未真机验证；空环境不会插入示例。

云函数未部署、环境ID为空，管理员与实际存储规则未配置。不得将本地测试等同于已上线或已完成跨用户同步。现有相册原生滑动、位置与分享真机检查仍待完成。连接与验收见 BLESSINGS.md。

## 既往记录


31组逻辑检查通过（check.js 23组、check-albums-schedule.js 7组、check-preview.js 1组）。覆盖默认动效、滚动状态节流及清理、菜单跳转、视频未配置/播放/进度/错误/超时/重播/后台退出、地图授权拒绝与恢复、请求过期、距离和目的地切换、流程时间、相册路由及资源隔离、媒体本地副本生命周期、原封面哈希及64张照片资源。60张下载原片逐一完整解码、检查读取稳定性、保留副本SHA256；四张用户指定照片可追溯并实际打开。

官方WXML/WXSS编译通过，包含五个相册分包与共享模板。官方CLI上传及预览均成功，平台包大小完全一致：总包7,971,513字节，主包1,173,772字节，所有单包小于2MB。

Web在390和320像素宽度无水平溢出，六个菜单在320像素窄屏保持48×48像素触摸区域。原封面、敦煌入口、相册首位、大图翻页和四张指定照片已实际检查；页面0运行错误。另以隔离的本地8秒测试片验证手动播放、结束返回、重播及关闭（首页和菜单均不会自动打开播放器）；缺失媒体测试验证失败提示和继续请柬。测试视频不在交付项目或上传包中。

微信模拟器已验证：新版首页入口与喜印、底部菜单、光影誓言首位、心动如初17张相册、用户指定两张照片、选中大图2/17、指南针查看距离到更新位置状态。使用模拟器测试位置，图中距离不代表用户位置。本轮模拟器拖动未切换照片，原生滑动翻页需在真机复核；Web左右翻页已验证。原有地点、真实手机路线和分享仍以真机验证为准。

工具无应用运行错误；保留getSystemInfo、SharedArrayBuffer、worker reportRealtimeAction警告，以及一次onReady 60ms和相册onHide 246ms性能提示。动画在后台暂停，系统减少动态效果样式已配置；不同真机表现仍需检查。

证据在work/invitation-polish，交付预览在exports。真实敦煌成片、媒体存储地址和海报尚未提供；当前明确显示敬请期待，相关真机媒体性能尚未验证。开发版1.3.0已上传，未提交审核或正式发布。

## 玫瑰藤蔓试版

素材原始输出2172×724 RGBA，项目副本840×280 RGBA，透明通道有效，增加135506字节。照片源与封面哈希检查、相册资源隔离及媒体预览生命周期检查通过；Web JavaScript语法、原生WXML/WXSS和官方预览编译通过。平台试版总包8110382字节，主包1312641字节，各分包不变且低于2MB。

Web连续相册已在390和320像素检查，无横向溢出。心动如初17张照片、16条装饰；藤蔓位于相邻照片空隙，pointer-events为none，装饰对辅助技术隐藏；点击第二张并切换第三张已验证，0页面错误。减少动态效果和后台暂停样式已配置。当前电脑控制微信窗口间歇返回noWindowsAvailable，未声称已完成原生藤蔓页面目视验证；提供独立微信试版预览码用于真机观看。未覆盖开发版1.3.0上传。

## 影像章节与题字更新

原生与Web同步六项菜单，独立影像在邀请正文之后。31组逻辑检查通过；WCC 7份模板与WCSC 9份样式通过。390/320像素无水平溢出；隔离8秒测试视频验证首页不播放、影像菜单只定位、点击才播放、结束及手动关闭停留影像、关闭后暂停。原生模拟器已显示原封面、新字体与六项菜单。字体13,424字节，以53个字符子集打包；最终成片与真实手机字体/播放仍待验证。

微信模拟器补充实看：点击“影像”已滚动至新章节，选中标记为影像；新标题正常显示，原图和我们章节顺序正确。证据：exports/小程序影像检查.jpg。

## 十款玫瑰藤蔓更新

34组逻辑检查通过：原31组加3组装饰分配与真实相册加载检查。100个不同种子、每条113处连接，验证同种子稳定、每轮十款唯一、跨轮不相邻重复；Web和原生分配一致；各相册最后一张之后无装饰。

十个生成原件均为RGBA、有0–255真实透明通道；交付PNG缩放为660×220并压缩，保留透明背景，合计255095字节，十个哈希各不相同。320/390像素连续相册无横向溢出，17张照片对应16段装饰、前十段十款各一次；装饰pointer-events:none。Web实点第2张放大并切到第3张、关闭成功，页面0运行错误。十款总览的十张图片均加载成功，透明底无棋盘格。

微信WCC 7份模板、WCSC 9份样式编译通过；预览成功，平台主包1453932字节，总包8251673字节，各分包低于2MB。模拟器已实看主相册首位下的回旋款；全量真机播放、分享与原生大图滑动复核仍沿用之前待验证范围。

模拟器相册实看补充：光影誓言第1/2张之间为花束长卷，第2/3张之间为月弧相拥，两段透明背景正确，照片之间保持留白。证据：exports/小程序相册-随机连接.jpg。十款总览交付为可滚动HTML页面；浏览器长截图拼接异常，未作为交付图片。

## 1.4.0发版准备（2026年9月8日）

重新运行全部34组逻辑检查，均通过。当前生产代码未再修改，官方CLI以AppID wx187416f73e2e53c3成功上传1.4.0。平台总包8,251,673字节、主包1,453,932字节，与上一轮十款藤蔓预览一致，所有单包低于2MB。后台名称、类目、隐私保护指引、定位接口资格、备案状态仍待管理员核对；本轮未提交审核或正式发布。

真实手机定位、路线、相册原生左右滑动和分享仍未完成验收。最终敦煌视频缺失，1.4.0保留敬请期待，首发视频方案待用户确定。


## 2026-09-08 云相册

新增签名白名单、身份校验、失败状态、链接刷新与重试测试；HTTPS/本地混合预览顺序与清理测试。64张照片+7张封面上传下载逐张哈希一致。真实云函数和模拟器验证结果、真机待验范围见 CLOUD-MEDIA.md。官方预览总包1,491,303字节。
