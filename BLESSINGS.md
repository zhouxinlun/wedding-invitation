# 亲友祝福 · 即时展示与飘雪

2026-09-08：正式 AppID `wx0d45570c72a490e7`，当前云环境 `wanshifuyue-d4ghbtrbi0e9bd01b`。`weddingBlessings` 已重新部署，新投稿完成照片处理后直接公开，无需新人确认。正式开发版本上传、体验版、审核和发布仍未核实，本次生成的是预览码。

## 当前交互

- 仅保留“亲友祝福”，最近50条按创建时间倒序排列。列表固定640rpx，显示滚动条；列表外仍可继续滑动请柬。
- 投稿面板采用暖米色信纸、暗红寄信按钮和照片小卡。无自定义“我愿意…”勾选，也没有“我的心意”或“新人管理”入口。
- 照片提示：**新郎手机相册穷尽，请尽情上传新娘的丑照**。
- 每次最多3张照片、160字；点表情会直接追加到正文，允许多次加入，计入正文长度。昵称由用户点选微信昵称或自行填写，不静默读取昵称、头像、手机号。
- 提交成功立即进入公共列表和飘雪；其他亲友前台每20秒刷新，非即时推送。失败保留原稿，结果不明时用相同投稿标识核对重试，避免重复发送。
- 仅一级祝福正文参与六种轻透明的字串飘落，回复不进入飘雪；六种样式交错落下：弧线、斜线、珍珠串、垂挂、波浪双线与折线花结。单条取前10个字作装饰，完整内容保留在列表；最多同时6条，透明度30%—39%，不接收触摸。
- 填写、看影像、进入地图或切到后台时暂停飘雪；系统“减少动态效果”偏好会隐藏装饰。

`web/blessings-preview.html` 含8条明确标注“示例”的 Mock 内容，投稿只在内存中展示，不写入云端。主 Web 请柬不加载 Mock，也未接入网页云登录；真实投稿通过小程序。

## 回复提交与本人删除（最新）

手机端回复改用原生 form 提交，按按钮时读取昵称和正文的当前值，防止输入/失焦更新尚未同步。所有回复错误放在固定发送区、按钮上方，空字段时能看到提示；发送中保留加载锁，成功后等旧轮询结束，再获取新的回复列表，避免旧数据覆盖刚送出的回复。结果不明仍沿用原 nonce 重试。

自己的祝福旁显示“删除祝福”，自己的回复旁显示“删除”，自己上传的每张照片下面显示“删除照片”。删除前确认范围，处理中锁定按钮；删除整条祝福会连同照片和回复一起从列表移除。单条回复删除保留其他回复，单张照片删除保留文字及其余照片；纯照片祝福的最后一张删掉后，空祝福也一起移除，确认框会说明。

云端继续以微信 OPENID 推导作者身份。新增 removeReply/removePhoto 两个事务动作：回复删除清空正文并保留内部 deletedAt 标记用于幂等和防重试复活，不进入公共结果；其他人已有的回复保留。照片用服务端生成的 photoKeys 稳定标识，不能按客户端文件路径删除，避免并发删除错位；photos URL 数组继续兼容旧预览。没有增加集合、索引或管理页面。照片源清理复用原删除机制；已取得的临时链接与缓存不保证立即失效，既有缓存核对事项仍适用。

本轮67组检查通过。实际点击原生“送出回复”成功，回复显示在正确的临时祝福下面；错误提示在按钮上方可见，删除确认与取消已实看。实际云调用把临时祝福的2张照片删成1张，正文和回复保留；删除回复后正文与照片仍在；最后删除整条临时祝福，列表和飘雪均不再包含它。原有3条亲友内容逐项核对保留。不同真实微信账号/手机仍需新预览码验收，证据在 work/blessing-delete/。

最新手机码：`exports/正式账号-回复与删除-预览码.jpg`；官方总包1519115字节，主包1517521字节。本轮已部署云函数，未执行正式代码上传、提审或发布。

## 自适应照片、加载与回复（2026-09-08 更新）

上传压缩先读取原始尺寸，只设置长边，最长1280px且不放大小图；不再把宽高同时设成1280。服务端重新编码保留收到的比例。选图和公共列表按原比例缩进较小的宽高范围，不裁切；点击仍可放大。此前已经被压成方形的文件无法从现有副本恢复，需要重新选择原照片上传。

发送期间显示显眼的旋转进度提示：整理照片、上传第几张、服务端送达、更新留言；期间禁用重复发送和编辑。按钮加载与进度面板同时存在，失败保留原稿重试。

点击一级祝福正文或“回复”按钮向作者回复；点击某条回复则向那条回复的作者回复。回复区显示“甲 回复 乙：正文”，按回复时间正序接在原祝福图片下。回复支持文字与正文表情，不参与飘雪，不改变一级祝福的时间倒序。

回复沿用云函数和原数据库，在原祝福中保存受事务保护的 replies 数组，不另建集合/索引。目标姓名由服务端按原祝福或原回复取得，客户端不能替换目标称呼。公开输出不含 OPENID、作者内部标识及重试指纹；昵称仍是用户确认的显示名，不是实名验证。

单条回复最多160字，每条祝福最多200条回复；每人回复间隔3秒、每天100条，与一级祝福的限额分别记录。重复请求复用相同 nonce，只计一次。原祝福不公开或被移除后，不能继续回复，附属回复也不再公开。容量满时返回明确提示，不静默丢失。

本轮真实云验证：320×180横图与120×240竖图上传后尺寸保持不变；一级正文含玫瑰；回复甲指向原作者、回复乙指向回复甲。原生实际点击留言打开了正确对象的回复面板。测试祝福与回复已移除，原有两条亲友留言保留。加载状态由实际发送调用确认；持续加载的布局另用原生暂存状态截图，未人为延长生产请求。官方预览总包1,502,307字节、主包1,500,713字节，仍未正式上传/提审/发布。详细证据在项目外 work/blessing-replies/。

开发者工具记录一条基础库内部 `__subPageFrameEndTime__` 错误，发生于重载，未定位到业务代码；本轮发送、读取、回复、照片解码均成功。已修正组件中禁用按钮的属性选择器警告。不同微信账号与真实手机仍待验收，不能用同一开发者会话替代。

## 云端数据与维护

沿用服务端 `approved` 表示已公开。提交仍经过占位、照片处理、完成三个步骤；未完成或失败记录不进入公共列表。新投稿不再要求 `consent`，不再生成 `consentAt`。部署前盘点待确认记录为0，无需迁移历史投稿。

云函数使用微信 `getWXContext()` 检查 OPENID 和 AppID。普通投稿不依赖管理员环境变量。数据库 `wedding_blessings` 与 `wedding_blessing_limits` 保持客户端禁止读写，由服务端访问；状态/时间与作者/时间索引保持不变。

存储仍为仅创建者可读写。用户上传自己的暂存文件，服务端校验并重新编码为独立 JPG，移除 EXIF/GPS 元数据，然后公开。公开照片沿用五分钟签名，签名链接到期前可能仍能访问。相册签名白名单与一小时链接不受本轮修改影响。

保留原有服务端身份受控的历史查询、隐藏、移除维护接口，供兼容旧记录与清理使用；用户页面不展示管理入口。新人 OPENID 仅保存在云端环境变量和项目外私有配置。不要用公开 `cloudbaserc.json` 覆盖现有环境变量。当前维护身份已配置，但不再是普通亲友投稿的前置条件。

投稿成功清理暂存文件；隐藏/移除先停止公开，再清理副本。逐文件删除持续失败会记录 `BLESSING_PHOTO_CLEANUP_PENDING`，不会恢复公开。临时 URL 缓存是否立即消失不作保证；过期暂存文件定时清理和婚礼后归档尚未实现。

## 本轮验证

- 58组本地检查通过：主逻辑23、相册/流程7、图片预览2、藤蔓3、云祝福14、飘雪与原生组件3、云相册2、编辑/压缩/回复交互4。
- 实际云调用：从原生投稿组件发送一条带合成测试图片的祝福，无勾选、无确认操作，返回 `approved`；公共列表立即出现，飘雪引用同一记录。照片下载200且可解码。随后移除该测试记录，公共列表恢复为空。
- 实际原生界面：新信纸表单、精确照片文案、无勾选、单一列表与6种字串可见。8条示例下列表实测高332px（390px设备宽度，640rpx），内部 scrollTop 从0到332时父页面未移动；在列表外滑动后父页面位置改变，列表内部位置保持332。
- Web脚本与样式检查通过；本轮本地演示页被浏览器 URL 安全策略阻止，未绕过，也未声称本轮 Web 视觉验收完成。原生模拟器临时示例已清空，未写入云数据库。
- 当前预览码：`exports/正式账号-祝福回复-预览码.jpg`。测试证据在项目外 `work/blessing-snowfall/`，不包含在上传包中。

两部真实手机的昵称选择、选图/拒绝授权、跨微信同步和照片隔离仍待验收。照片选择仍遵循微信平台隐私授权流程；移除的是额外的自定义勾选。

## 发布时同步的用途说明

用户主动投稿时，处理其填写或选择的称呼、祝福文字、表情和照片，用于向查看婚礼请柬的亲友展示，并生成祝福飘雪。微信 OPENID 用于识别投稿归属与限制重复投稿，不向其他亲友展示。用户可不投稿而继续浏览；内容清理由维护方处理。保留现有位置用途说明；不读取头像、手机号、通讯录或整个相册，仅处理主动选择的照片。

当前未接入自动内容审核。公众平台类目、用户生成内容及隐私指引需按最终功能核对，不能将本地测试视为审核通过。

## 部署与复验

当前云函数实际运行于 Nodejs16.13，512MB、60秒，本轮调用成功；升级到项目 `engines >=18` 要求的运行时仍待完成。部署配置目标 Nodejs20.19 不代表云端升级已执行。

```sh
/Applications/wechatwebdevtools.app/Contents/MacOS/cli cloud functions deploy --env wanshifuyue-d4ghbtrbi0e9bd01b --names weddingBlessings --project '/Users/zhouxinlun/Documents/marry me/invitation-app' --remote-npm-install
node scripts/check-blessings.js
node scripts/check-blessing-snow.js
node scripts/check-blessing-editor.js
node scripts/check-cloud-media.js
node scripts/check.js
node scripts/check-albums-schedule.js
node scripts/check-preview.js
node scripts/check-vines.js
```

本地云函数测试使用真实入口和图片编解码，SDK身份、数据库与存储用替身；与本轮实际部署验证分别记录。手机验收流程为亲友A投稿 → A立即看到 → B在下一轮更新看到并飘雪，另测选图拒绝、后台返回、失败重试与列表内外滚动。
